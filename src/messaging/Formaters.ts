import { EmbedFieldData, MessageEmbed, User } from "discord.js";

// export interface TableHeader {
//   name: string;
//   variable: string;
//   width?: number;
// }

// export const Table = (headers: Array<TableHeader>, rows: Array<any>): Array<EmbedFieldData> => {
//   const widths = headers.map(() => 0);
//   const assembleTableRow = (row: Array<string>) => {};
//   const result: Array<EmbedFieldData> = [];
//   const data = rows.map((row) =>
//     headers.map((header) => {
//       return row[header.variable];
//     })
//   );
//   data.forEach(() => result.push(assembleTableRow()));
//   return result;
// };
